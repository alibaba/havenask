class SampleScorer {
    SampleScorer () { return; }
    int score(int val1, int val2) { return val1 + val2; }
    static SampleScorer createSampleScorer() {
	SampleScorer sample = new SampleScorer();
	return sample;
    }
}
