#include "indexlib/merger/split_strategy/simple_split_strategy.h"

using namespace std;

IE_NAMESPACE_BEGIN(merger);
IE_LOG_SETUP(merger, SimpleSplitStrategy);

const std::string SimpleSplitStrategy::STRATEGY_NAME = "simple";

IE_NAMESPACE_END(merger);

